apiVersion: v1
kind: Service
metadata:
  name: argoapp
  labels:
    app: argoapp
spec:
  selector:
    app: argoapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
